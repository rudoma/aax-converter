# Deine Audible activation bytes (8 hex-Zeichen) – Pflichtfeld
# Ermitteln z.B. mit: audible-cli (pip install audible-cli)
ACTIVATION_BYTES=abcdef12

# Lokaler Pfad, in dem die fertigen Dateien abgelegt werden
OUTPUT_PATH=./output

# Port auf dem Host (Standard: 8080)
PORT=8080
